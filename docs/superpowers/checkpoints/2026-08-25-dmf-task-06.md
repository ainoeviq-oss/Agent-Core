# DMF Task 06 — Deterministic Graph Linker

Date: 2026-08-25 (Asia/Jakarta)

## RED evidence

- `tests/memory-linker.test.ts` was written first.
- Initial run exit `1` because `src/memory/linker.ts` did not exist.

## GREEN evidence

- Relation weights follow the master plan: same key/supersedes/explicit 1.00, same anchor 0.95, same artifact 0.90, same route/task 0.80, co-occurs 0.60, bounded token overlap 0.20..0.55, temporal neighbor 0.15..0.30.
- Supersedes/explicit relations are accepted only as structured relation evidence; the automatic linker never infers them from prose.
- Candidate discovery is principal/project scoped, state filtered, deterministically ordered, and capped; no global O(N²) comparison is performed.
- Token overlap uses deterministic Unicode token sets and Jaccard thresholding.
- Temporal linking requires the same thread or resource inside a configured bounded window.
- Associative evidence edges are inserted both directions with source-event evidence and `INSERT OR IGNORE` idempotence.
- Re-running the linker on unchanged evidence produced zero new edges.
- Golden unrelated prose produced no edge.
- Focused test: 2/2 passed, exit 0.
- Build: exit 0.
- Full regression: 37/37 files and 131/131 tests passed, exit 0.
