# DMF Task 02 — Types, Configuration, and Schema

Date: 2026-08-25 (Asia/Jakarta)

## RED evidence

- `tests/memory-schema.test.ts` was created before memory modules existed.
- Initial run exit `1` because `../src/memory/types.js` did not exist.
- Log: `runtime/dmf-execution/logs/task2-red.log`.

## GREEN evidence

- Public contracts define the plan's minimum memory kinds/states and Section 10 relation classes, plus enforcement `none|soft|hard`.
- `MemoryScope` carries `principalId` with optional `projectId`, `threadId`, and `resourceId`.
- Commit/revise/search/preflight/explain result contracts are present.
- Config defaults source the plan's fixed values: graph caps 1,000/10,000, two hops, PPR 0.85/1e-6/20, and score weights 0.40/0.20/0.20/0.08/0.07/0.05.
- Plan-unspecified bounded defaults were selected explicitly: seed cap 64, recall item/character budgets 24/12,000, busy timeout 5,000 ms, token Jaccard threshold 0.35, temporal window 30 minutes, observation archive horizon 90 days, tombstone retention 30 days.
- Migration `001_initial_memory` creates canonical tables, migration metadata, deterministic indexes, `memory_anchors`, and FTS5 projection.
- Migration is idempotent; foreign-key enforcement and `PRAGMA user_version=1` are tested.
- Deterministic FTS rebuild produced identical results on consecutive rebuilds.
- Focused test: 2/2 passed, exit 0.
- Build: exit 0.
- Full regression: 31/31 files, 121/121 tests passed, exit 0.
