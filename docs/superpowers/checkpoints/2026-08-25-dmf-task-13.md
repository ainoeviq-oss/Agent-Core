# DMF Task 13 Checkpoint — MCP memory tools

Date: 2026-08-25 (Asia/Jakarta)
Branch: `feature/deterministic-memory-fabric`
Plan task: Task 13 — MCP memory tools

## Scope completed

- Registered exactly eight deterministic memory MCP tools: `memory_status`, `memory_search`, `memory_get`, `memory_commit`, `memory_revise`, `memory_forget`, `memory_explain`, and `memory_export`.
- Added bounded Zod schemas and read/write/destructive annotations.
- Bound every tool scope to the authenticated principal and current project root.
- `memory_commit` and `memory_revise` require explicit structured fields.
- `memory_forget` performs soft tombstoning only; no model-facing physical purge parameter exists.
- Search/export limits are bounded.
- Added provenance-safe get/explain/export views; raw event text is not exposed.
- Added principal isolation, malformed-input, revision/provenance, explainability, export, and tombstone tests.
- MCP server now exposes 31 tools total (23 existing + 8 memory tools).

## TDD evidence

- RED: `runtime/dmf-execution/logs/task13-red.log` — 3/3 focused tests failed before memory tools were registered.
- Intermediate GREEN: `task13-green-focused.log` reached 2/3; the remaining assertion incorrectly required anchor evidence specifically.
- Corrected acceptance assertion to require a positive score plus any valid deterministic match signal (anchor, lexical, or exact), matching the plan's explainability requirement.
- Final focused test: `tests/mcp-memory.test.ts` — 3/3 PASS.
- Regression cluster: 5 files / 10 tests PASS.
- TypeScript build: PASS.
- Full regression: 44/44 test files, 150/150 tests PASS, exit 0.

## Safety / invariants

- `git diff --check`: exit 0.
- No model-facing purge parameter.
- No `raw_text` / `rawText` export field.
- No external network I/O in the memory MCP/service changes.
- No embedding/reranker/vector/model dependency introduced.
- Existing launcher, tray-manager, OAuth, routing, operational audit, and route-enforcement suites remain green.

## Commit target

`feat: expose deterministic memory over agent core mcp`
