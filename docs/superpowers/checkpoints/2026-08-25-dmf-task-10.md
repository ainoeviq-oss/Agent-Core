# DMF Task 10 — Memory Service Facade and Preflight

Date: 2026-08-25 (Asia/Jakarta)

## RED evidence

- `tests/memory-preflight.test.ts` was written before runtime memory wiring. Initial run exited `1`; both tests failed because `runtime.memory` was undefined.
- Runtime lifecycle wiring was separately test-driven: the index close hook was removed, a new runtime test was added, and the RED run exited `1` because `service.memory` was undefined while existing listener tests stayed green.

## GREEN evidence

- `MemoryService` is constructed exactly once by `createRuntimeServices` and exposed as `runtime.memory`.
- Disabled/default memory is lazy and inert: no DB is opened and preflight returns an explicit empty evidence result instead of invented recall.
- Enabled memory lazily constructs one worker/store/retriever/linker/lifecycle/preflight component set and enters a healthy/degraded state based on factual open results.
- `MemoryPreflightEngine` retrieves deterministic evidence, active hard guardrails, open conflicts, prior failures, and related decisions; it persists `memory_contexts` and `memory_access_log` rows with route TTL, query hash, result JSON, blocking JSON, and snapshot hash.
- Repeated identical preflight against unchanged evidence returned identical ordered memory IDs and snapshot hash.
- Service facade exposes bounded memory operations without arbitrary SQL; context inspection is principal-bound.
- `MemoryService.close()` is idempotent. `startAgentCoreService` now exposes the memory facade, passes the configured memory policy, and closes the initialized memory worker with the HTTP listener.

## Verification

- Focused preflight suite: 2/2 passed.
- Combined preflight/runtime lifecycle gate: 2 files / 5 tests passed, exit 0.
- Build: exit 0.
- `git diff --check`: exit 0.
- Full regression: 41/41 files, 140/140 tests passed, exit 0.
- Static audit: `service.ts`/`preflight.ts` contain no network client, external AI/model, embedding, reranker, or vector-service dependency.
