# DMF Task 04 — Redaction, Normalization, and Anchors

Date: 2026-08-25 (Asia/Jakarta)

## RED evidence

- Three focused tests were written before implementation.
- Initial focused run exit `1`; `redaction.js`, `normalizer.js`, and `anchors.js` did not yet exist.

## GREEN evidence

- Deterministic redaction covers bearer authorization, API/client/access keys, passwords, and token fields using explicit patterns.
- Normalization uses Unicode NFKC, whitespace collapse/trim, and a lower-case search projection.
- Anchor extraction covers quoted/unquoted Windows paths, Unix paths, UUIDs, capability IDs, Agent Core tool IDs, URLs, explicit `key:value`, process IDs, run IDs, and route IDs.
- Anchor output is deduplicated and stable-sorted.
- Redaction markers and values removed by redaction are not emitted as anchors.
- Golden coverage includes Indonesian/English text, punctuation, a Windows path containing spaces, repeated UUIDs, and structured IDs.
- Focused tests: 3 files / 5 tests passed, exit 0.
- Build: exit 0.
- Full regression: 35/35 files and 128/128 tests passed, exit 0.
