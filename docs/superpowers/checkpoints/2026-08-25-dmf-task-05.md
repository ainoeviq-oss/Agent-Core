# DMF Task 05 — Event Journal and Versioned Store

Date: 2026-08-25 (Asia/Jakarta)

## RED evidence

- `tests/memory-store.test.ts` was written first.
- Initial run exit `1` because `src/memory/store.ts` did not exist.

## GREEN evidence

- Store opens/migrates only through `MemoryWorkerClient`; executable `DatabaseSync` remains confined to `db-worker.ts`.
- `memory_events` has append-only UPDATE/DELETE abort triggers.
- `recordEvent` persists only redacted text; `raw_text` is NULL by default.
- Structured metadata/value sanitization redacts known secret-bearing keys before stable JSON serialization.
- Semantic commit writes source event + item/revision + anchors + FTS projection in one ordered worker transaction.
- Same scope/key + equal hash deduplicates while still appending evidence; different authorized value creates a new revision with `valid_to` and `supersedes_revision_id` chain.
- Missing explicit-relation target causes FK failure and proves full transaction rollback: neither event nor item survives.
- `getMemory`, revision listing, revise, and tombstone are principal/project scoped; cross-principal operations are denied as not found.
- Tombstone writes an audit event and removes the item from FTS without deleting journal evidence.
- `git diff --check` exit 0; executable DatabaseSync audit remained worker-only.
- Focused test: 1/1 passed, exit 0.
- Build: exit 0.
- Full regression: 36/36 files and 129/129 tests passed, exit 0.
