# DMF Task 15 Checkpoint — Awareness evaluation harness

Date: 2026-08-25 (Asia/Jakarta)
Branch: `feature/deterministic-memory-fabric`
Plan task: Task 15 — Evaluation harness for awareness

## Scope completed

Created `tests/memory-awareness.acceptance.test.ts` and `tests/fixtures/memory-awareness/scenarios.json` with executable acceptance coverage for all required scenarios:

- Scenario A: a two-year-old pinned hard project guardrail outranks fresh related chatter.
- Scenario B: a changed decision recalls the newest active revision while revision history remains auditable.
- Scenario C: a failure from an earlier run appears in preflight before the same action is repeated.
- Scenario D: deterministic graph recall follows project → artifact → decision → tool constraint across multiple hops.
- Scenario E: two unrelated projects under the same principal do not cross-contaminate recall.
- Scenario F: ambiguous free-form contradiction is surfaced as an open conflict while both branches are preserved.
- Scenario G: identical route queries return identical ordered memory/revision IDs and snapshot hash.
- Scenario H: a real Agent Core service restart rehydrates the same DB and returns the same recall IDs/hash.
- Additional invariant: mutable structured memory cannot be silently overwritten without explicit revision authority.

## Production gap found and fixed

Initial acceptance run reached 8/9 PASS. Scenario D recalled the terminal constraint but assigned graph score 0. Root cause: `explicit_relation` was persisted only in the caller → target direction; neighborhood discovery traversed both endpoints, but directed PPR could not propagate from the project seed toward downstream nodes.

The master plan marks only `supersedes` as directed (old → new). `explicit_relation` is therefore now persisted bidirectionally, while `supersedes` remains directed. This makes explicit associative graph evidence traversable by weighted PPR from either side without changing revision semantics.

## Evidence

- Initial acceptance: 8/9 PASS; only Scenario D failed on graph score.
- After relation fix: awareness harness 9/9 PASS.
- TypeScript build: PASS.
- Graph/store/retrieval/recovery regression cluster: 7 files / 25 tests PASS.
- `git diff --check`: exit 0.
- Full regression: 46/46 test files, 163/163 tests PASS, exit 0.

## Safety / invariants

- No new AI/model/vector dependency.
- No external network dependency.
- Scope isolation remains principal + project bounded.
- `supersedes` remains the only explicitly directed relation from the master contract.
- Launcher, tray, OAuth, MCP, routing, recovery, and operational audit regressions remain green.

## Commit target

`test: prove deterministic long-term memory awareness`
