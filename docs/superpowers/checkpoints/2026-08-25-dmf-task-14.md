# DMF Task 14 Checkpoint — Persistence, backup, recovery, and integrity

Date: 2026-08-25 (Asia/Jakarta)
Branch: `feature/deterministic-memory-fabric`
Plan task: Task 14 — Persistence, backup, recovery, and integrity

## Scope completed

- Preserved SQLite worker ownership: production `DatabaseSync` execution remains inside `src/memory/db-worker.ts`.
- Startup open already enforced `PRAGMA quick_check`; Task 14 now also runs post-migration `PRAGMA integrity_check` and fails the memory subsystem closed/degraded on failure.
- Added forward-only schema version gate; databases newer than the runtime are rejected.
- Existing databases with an older `user_version` receive an automatic consistent timestamped pre-migration backup before schema mutation.
- Added live consistent backup through worker-owned `VACUUM INTO`, using `runtime/memory/backups` next to the DB and an atomic `last-backup.json` manifest.
- Added restore procedure that requires the Agent Core memory service to be stopped, validates the source backup, creates a pre-restore safety backup, stages in the same directory, atomically renames, validates the restored database, and rolls back on failure.
- Added explicit `PRAGMA wal_checkpoint(TRUNCATE)` command and clean-close checkpoint behavior.
- Added `memory_status` metadata for DB bytes, last integrity check, latest successful integrity check, last backup path, and backup time.
- Added crash-recovery acceptance proving committed data survives while an uncommitted WAL transaction from a terminated writer process is rolled back.
- Added corrupt-database acceptance proving memory reports degraded/closed behavior instead of returning invented context.

## TDD evidence

- RED: `tests/memory-recovery.test.ts` initially failed at import because `src/memory/backup.ts` did not exist.
- Focused GREEN: `tests/memory-recovery.test.ts` — 4/4 PASS.
- TypeScript build: PASS.
- Regression cluster: 6 files / 15 tests PASS.
- Full regression: 45/45 test files, 154/154 tests PASS, exit 0.

## Safety / invariants

- `git diff --check`: exit 0.
- No external network I/O added.
- No memory model, embedding, reranker, vector DB, or hidden AI dependency added.
- Backup and restore paths remain under the configured runtime memory directory by default.
- Restore requires explicit stopped-service authority; model-facing `memory_forget` still exposes no physical purge.
- Existing launcher, tray-manager, OAuth, routing, MCP, operational audit, and memory tool suites remain green.

## Commit target

`feat: harden memory persistence and recovery`
