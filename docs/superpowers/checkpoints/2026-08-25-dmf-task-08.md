# DMF Task 08 — Hybrid Retriever and Score Explanation

Date: 2026-08-25 (Asia/Jakarta)

## RED evidence

- `tests/memory-retriever.test.ts` was written before production modules.
- Initial run exit `1` because `src/memory/scoring.ts` did not exist.
- Log: `F:\Projects\Agent-Core\runtime\dmf-execution\logs\task8-red.log`.

## GREEN / debugging evidence

- Score composition uses the configured Section 11 weights: lexical 0.40, exact 0.20, graph 0.20, state 0.08, importance/pin 0.07, recency/access 0.05.
- Retrieval unions exact canonical/anchor matches and FTS5/BM25 seeds under principal/project scope.
- Graph expansion is bounded by configured hops/nodes/edges and feeds deterministic PPR.
- Active hard guardrails are ordered before ordinary ranked evidence when they match the candidate set.
- `whyMatched` exposes component scores, lexical terms, matched anchors, and graph paths.
- Candidate families are current-revision rows only; tombstoned/superseded items are excluded from default recall.
- Evidence packing obeys configured item and character budgets for ordinary memory; hard guardrails are not silently dropped by ordinary ranking.
- Snapshot hashes are SHA-256 over deterministic ordered recall evidence.
- First GREEN attempt exposed two implementation issues: generic row interfaces violated the worker client's `Record` constraint, and direct graph provenance was lost when both linked nodes were also lexical seeds. Both were fixed in production code without weakening tests.

## Final verification

- Focused test: 2/2 passed, exit 0.
- Build: exit 0.
- `git diff --check`: exit 0.
- Full regression: 39/39 files and 135/135 tests passed, exit 0.
- Retriever/scoring source audit: no external I/O call and no model/vector runtime term detected.
