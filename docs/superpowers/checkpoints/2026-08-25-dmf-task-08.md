# DMF Task 08 — Hybrid Retriever and Score Explanation

Date: 2026-08-25 (Asia/Jakarta)

## RED evidence

- `tests/memory-retriever.test.ts` was written before `src/memory/scoring.ts` and `src/memory/retriever.ts` existed.
- Initial focused run exit `1` because `../src/memory/scoring.js` could not be resolved.

## Implementation/debugging evidence

- Exact-anchor/key seeds and FTS5/BM25 lexical seeds are unioned and capped deterministically.
- Candidate graph expansion is principal/project scoped, bounded to configured hops/nodes/edges, then ranked with the Task 7 PPR engine.
- Section 11 score weights are applied exactly: lexical 0.40, exact 0.20, graph 0.20, state/guardrail 0.08, importance/pin 0.07, recency/access 0.05.
- Active hard guardrails are included ahead of ordinary ranked memories.
- `whyMatched` records lexical terms, matched anchors, graph path evidence, state/importance/recency components, and final score.
- Snapshot hash is SHA-256 over the deterministic ordered result projection.
- First GREEN attempt exposed two implementation defects: generic query row typing violated the worker client's `Record<string, unknown>` constraint, and direct graph provenance was omitted when a linked memory was also a lexical seed. Both were fixed in production code without weakening tests.

## Final verification

- Focused retriever tests: 2/2 passed, exit 0.
- Build: exit 0.
- `git diff --check`: exit 0.
- Full regression: 39/39 files and 135/135 tests passed, exit 0.
- Static audit: retriever/scoring imports contain no network client, model runtime, embedding/vector service, or hidden AI dependency.
