# GitHub Actions is permanently disabled for this repository.
# CI execution is forbidden by Agent Core repository policy.
# Local verification is authoritative; this file intentionally defines no workflow.
